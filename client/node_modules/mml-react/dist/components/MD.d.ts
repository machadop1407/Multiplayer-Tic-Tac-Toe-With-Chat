import { FC } from 'react';
export declare const truncate: (input: string, length: number, end?: string) => string;
export declare type MDProps = {
    /** The markdown text */
    text: string;
};
/**
 * MD renders a given text as markdown
 */
export declare const MD: FC<MDProps>;
